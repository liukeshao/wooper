logging:
  level:
    root: info
    org.ktorm: debug

server:
  port: 60000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wooper
    username: root
    password:
  redis:
    host: localhost
    port: 6379
sa-token:
  # token?? (????cookie??)
  token-name: satoken
  # token??????s ??30?, -1??????
  timeout: 2592000
  # token????? (???????????token??) ??: ?
  activity-timeout: -1
  # ???????????? (?true???????, ?false?????????)
  is-concurrent: true
  # ?????????????????token (?true?????????token, ?false?????????token)
  is-share: false
  # token??
  token-style: uuid
  # ????????
  is-log: false